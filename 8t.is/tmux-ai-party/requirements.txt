# Core dependencies
openai>=1.60.0  # Latest OpenAI Python SDK
python-dotenv>=1.0.0  # For environment variable management
libtmux>=0.40.0  # Python tmux session management
watchdog>=6.0.0  # File system monitoring (for config changes)
click>=8.1.0  # CLI framework
pyyaml>=6.0  # For YAML config files
colorama>=0.4.6  # Colored terminal output
ollama>=0.3.0  # Ollama API for local LLMs - Trisha's budget-friendly choice!
paramiko>=3.4.0  # SSH connections for remote tmux - tai.is magic! 🌐
cryptography>=42.0.0  # For secure auth in tai.is
pyjwt>=2.8.0  # JWT tokens for tai.is authentication
paramiko>=3.5.1 
colorama

# MCP Server dependencies 🌟
# mcp is a different install istelf  https://pypi.org/project/mcp/
google-generativeai>=0.7.0 # For Google Gemini API integration
fastapi>=0.115.0  # Modern web framework for OpenAI MCP
uvicorn[standard]>=0.32.0  # ASGI server for FastAPI
sse-starlette>=2.1.0  # Server-Sent Events support

# Web interface dependencies 🎪
aiohttp>=3.9.0  # For web server
aiohttp-cors>=0.7.0  # CORS support
websockets>=12.0  # WebSocket support

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0  # For async test support
black>=24.0.0
flake8>=7.0.0
mypy>=1.0.0

